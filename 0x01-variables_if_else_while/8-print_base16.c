#include<stdio.h>
/**
 * main - thos is my main function
 *
 * Return: Always returns zero
 */
int main(void)
{
	int j;
	char m;

	for (j = 0; j < 10; j++)
		putchar(j + '0');
	for (m = 'a'; m <= 'f' m++)
		putchar(m);
	putchar('\n');
	return (0);

}
