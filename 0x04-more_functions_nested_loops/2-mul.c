#include "main.h"
#include <stdio.h>
/**
 * mul - multiply two integers
 * @a: the first integer
 * @b: the second integer
 * Return: return 0 always
 */
int mul(int a, int b)
{
	int ans = a * b;

	return (ans);
}
